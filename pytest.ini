[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    smoke: critical-path tests that verify the API is reachable and healthy
    crud: create/read/update/delete lifecycle tests
    negative: tests for invalid input and error handling
